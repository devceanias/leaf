From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: plantball <mythicalplanetdude@gmail.com>
Date: Sun, 26 Apr 2026 01:22:30 +1000
Subject: [PATCH] add player update interval override


diff --git a/net/minecraft/server/level/ChunkMap.java b/net/minecraft/server/level/ChunkMap.java
index 21cbdbb67686cb67be66ea5f0e695b47c92823a9..24ad189bbde9de99f5216612e5552f4dea7a3af6 100644
--- a/net/minecraft/server/level/ChunkMap.java
+++ b/net/minecraft/server/level/ChunkMap.java
@@ -105,6 +105,7 @@ import net.minecraft.world.level.storage.DimensionDataStorage;
 import net.minecraft.world.level.storage.LevelStorageSource;
 import net.minecraft.world.phys.Vec3;
 import org.apache.commons.lang3.mutable.MutableBoolean;
+import org.dreeam.leaf.config.modules.opt.PlayerUpdateInterval;
 import org.jspecify.annotations.Nullable;
 import org.slf4j.Logger;
 
@@ -1011,6 +1012,15 @@ public class ChunkMap extends SimpleRegionStorage implements ChunkHolder.PlayerP
             i = org.spigotmc.TrackingRange.getEntityTrackingRange(entity, i); // Spigot
             if (i != 0) {
                 int updateInterval = type.updateInterval();
+
+                if (entity instanceof ServerPlayer) {
+                    final int configuredInterval = PlayerUpdateInterval.interval;
+
+                    if (configuredInterval > 0) {
+                        updateInterval = configuredInterval;
+                    }
+                }
+
                 if (this.entityMap.containsKey(entity.getId())) {
                     throw (IllegalStateException)Util.pauseInIde(new IllegalStateException("Entity is already tracked!"));
                 } else {
